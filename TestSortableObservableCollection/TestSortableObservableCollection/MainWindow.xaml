<Window x:Class="TestSortableObservableCollection.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestSortableObservableCollection"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <local:MyBindingProxy x:Key="DataContextProxy" Data="{Binding}" />
    </Window.Resources>
        
    <Grid>
        <Grid.Resources>
            <HierarchicalDataTemplate x:Key="GDSCommandSubgroupItemTemplate" ItemsSource="{Binding Children}" >
                <StackPanel>
                    <TextBlock Text="{Binding Description}" >
                        <TextBlock.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Add Subgroup" Command="{Binding Data.AddSubgroupCommand, Source={StaticResource DataContextProxy}}" >
                                    
                                </MenuItem>
                            </ContextMenu>
                        </TextBlock.ContextMenu>
                    </TextBlock>
                </StackPanel>
            </HierarchicalDataTemplate>
            
            <HierarchicalDataTemplate x:Key="GDSCommandItemTemplate" ItemsSource="{Binding Children}" >
                <StackPanel>
                    <TextBlock Text="{Binding Description}" Foreground="#FF28F128" >

                    </TextBlock>
                </StackPanel>
            </HierarchicalDataTemplate>

            <local:GDSCommandItemDataTemplateSelector x:Key="GDSCommandItemDataTemplateSelector" 
                GDSCommandSubgroupTemplate="{StaticResource GDSCommandSubgroupItemTemplate}" 
                GDSCommandItemTemplate="{StaticResource GDSCommandItemTemplate}"  >
                
            </local:GDSCommandItemDataTemplateSelector>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TreeView Grid.Row="1" ItemsSource="{Binding Root}" ItemTemplateSelector="{StaticResource GDSCommandItemDataTemplateSelector}" >
            
        </TreeView>
                  
    </Grid>
</Window>
